# Environment & secrets
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
venv/
.venv/
env/
ENV/

# Notebooks
.ipynb_checkpoints/

# Data — raw PDFs and processed artefacts
data/raw/*.pdf
data/processed/document_hashes.json
retrieval/bm25_index.pkl
# chunks.json is committed so Streamlit Cloud can rebuild the BM25 index at startup

# Evaluation outputs
evaluation/results/*.csv
evaluation/results/*.json

# Logs
*.log
logs/

# IDE
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store

# HuggingFace model cache (ingestion only — never at runtime)
~/.cache/huggingface/

# NLTK data downloaded at runtime (cached locally, not committed)
nltk_data/

# Claude Code
.claude/
CLAUDE.md
